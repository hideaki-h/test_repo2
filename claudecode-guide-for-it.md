# Claude Code 使い方ガイド（IT部門PC担当向け）

> [!info] このガイドについて
> IT部門のPC担当者がClaude Codeを使ってアウトプットを出せるようになること、
> そしてチームでAI駆動運用ができる状態になることを目的としています。

---

## 📅 3日間のレクチャー地図

3回×1時間のセッションで、毎回「できること」が1段ずつ増える構成です。各日の**ゴール（到達状態）**を意識して進めてください。

| 日 | 扱う章 | ゴール（到達状態）|
|---|---|---|
| **Session 1（月）** | 1〜4章（概要・セットアップ・基本操作・**ガードレール / トークン**・ログ分析）| **一人で実務に使える** |
| **Session 2（水）** | 5〜7章（ドキュメント作成・スクリプト生成・**Skill化**）| **繰り返し作業を効率化できる** |
| **Session 3（金）** | 8〜10章（**リポジトリ構成**・**VS Code**・Git / GitHub）| **チームで共有・運用できる** |

> [!note] 付録は全日共通
> **11章（安全に使うために・詳細版）** と **12章（プロンプト例集）** は、特定の日に読み切るのではなく、全セッションを通して必要なときに参照する付録です。

---

## 1. Claude Codeとは

### 1-1. AIチャットとの違い

| | ChatGPT / Claude.ai などのAIチャット | Claude Code |
|---|---|---|
| 操作場所 | ブラウザ | PC上のターミナル（黒い画面） |
| ファイルの扱い | テキストを貼り付けて渡す | PC上のファイルを直接読み込める |
| 得意なこと | 質問・相談・文章作成 | コード生成・ファイル操作・調査・自動化 |
| 実行 | 出力するだけ | コマンドを実行することもできる |

**一言でいうと：** AIチャットは「相談相手」、Claude Codeは「手を動かせるアシスタント」

### 1-2. 何が得意か・何が苦手か

**得意なこと**
- PowerShellスクリプトの生成・説明
- ログファイルを読み込んでエラーの原因を特定する
- 手順書・マニュアルをゼロから作る
- 既存のスクリプトを修正・改善する
- コードの動きを日本語で説明する

**苦手なこと・注意が必要なこと**
- 最新情報（知識のカットオフがある）
- 実環境の状態（画面を見ることはできない）
- 100%正確な出力の保証（必ず確認が必要）

---

## 2. セットアップ

### 2-1. インストール方法（Windows）

**前提：** PowerShellが使える状態であること（Windows 10/11は標準で使用可）。**管理者権限は不要**です。

**手順（推奨：ネイティブインストーラ）：**

1. PowerShellを開く（管理者でなくてよい）
2. 以下のコマンドを実行

```powershell
irm https://claude.ai/install.ps1 | iex
```

3. インストール完了後、ターミナルを再起動

> [!tip] なぜこの方法か
> 公式推奨の方式で、**Claude Code本体が自動でアップデート**されます。
> IT部門で複数人に配る場合、更新の手間がかからないのが利点です。

> [!info] wingetでも入れられる（ただし手動更新）
> 社内でwinget配布を統一している場合は次でも可：
> ```powershell
> winget install Anthropic.ClaudeCode
> ```
> ただし**wingetは自動更新されません**。更新時は `winget upgrade Anthropic.ClaudeCode` を手動で実行してください。

### 2-2. ログインと初期設定

```powershell
# ターミナルを開いてClaudeを起動
claude
```

初回起動時にブラウザが開き、ログインを求められます。

> [!warning] 無料プランでは使えません
> Claude Codeの利用には、**Pro / Max / Team / Enterprise のいずれか（有料）**、または
> Console（API従量課金）のアカウントが必要です。無料のClaude.aiプランには含まれません。
> **チームで運用するなら「Claude for Teams」が公式推奨**です（一元課金・メンバー管理つき）。
> 事前にどのプランで契約するか決めておいてください。

**ログイン後の確認：**

```
> こんにちは。使い方を教えてください
```

日本語で返答が来ればセットアップ完了です。

### 2-3. ⚠️ 最初に知っておくべきルール（ガードレール）

> [!danger] 全自動（bypassPermissions）は使わない
> Claude Codeには確認をすべてスキップして実行する「bypassPermissions」モード（`--dangerously-skip-permissions`）があります。有効にするとClaudeが**確認なしに**ファイルの書き換えやコマンド実行を行います。
> 
> **IT業務では使用しないでください。**
> Claudeが操作するたびに確認プロンプトが出る「通常モード」が正常な状態です。
> モードは `Shift + Tab` で切り替わるので、誤って変えてしまったら同じく `Shift + Tab` で通常に戻してください。

> [!warning] 渡してはいけない情報
> 以下の情報はClaudeに渡さないでください。Anthropicのサーバーに送信されます。
> - パスワード・認証情報
> - 個人情報（氏名・住所・マイナンバー等）
> - 社外秘・機密扱いのデータ
> - ライセンスキー・APIキー

> [!warning] 生成物は必ず確認してから使う
> 特にPowerShellスクリプトは、**実行前に必ず内容を読んで意図通りか確認**してください。
> Claudeが生成したスクリプトをそのまま実行して意図しない変更が起きた事例があります。

> [!info] 出力は「提案」である
> Claudeの回答は正しいとは限りません。
> ログ分析の結果も、生成したスクリプトも、「Claudeの提案」として受け取り、
> 最終判断は必ず人間が行ってください。

---

## 3. 基本の使い方

### 3-1. 指示の出し方（プロンプトのコツ）

**良い指示の3要素：**

| 要素 | 説明 | 例 |
|---|---|---|
| **何を** | 作りたいもの・やりたいこと | PowerShellスクリプト |
| **条件** | 具体的な制約・環境 | Windows 11、管理者権限不要 |
| **形式** | 出力の形式 | コメント付きで、日本語で説明も |

**悪い例 → 良い例：**

```
❌ スクリプト作って

✅ Windows 11のPCで、特定フォルダ以下のログファイル（.log）を
   30日以上前のものだけ削除するPowerShellスクリプトを作ってください。
   管理者権限なしで動作するもので、削除前に対象ファイルの一覧を表示してから
   確認を求めてください。コメントを日本語で入れてください。
```

**段階的に進める：**

一度に完成させようとせず、段階的に指示するのがコツです。

```
Step 1: まずスクリプトの骨格を作ってください
Step 2: エラーハンドリングを追加してください
Step 3: ログ出力機能を追加してください
```

### 3-2. ファイルを渡す方法

Claude Codeはプロジェクトディレクトリ内のファイルを直接読み込めます。

```powershell
# 作業フォルダに移動してからClaudeを起動する
cd C:\IT\scripts
claude
```

```
# Claudeへの指示例
このフォルダにある setup.ps1 を読んで、何をしているか日本語で説明してください
```

> [!tip] ファイルを渡すベストプラクティス
> - 作業対象のファイルがあるフォルダで `claude` を起動する
> - ファイル名を指示の中に明記する
> - 一度に大量のファイルを渡さない（1〜3ファイルずつ）

### 3-3. トークンとは

Claude Codeの「処理量」の単位が **トークン** です。文章をAIが扱う細かい断片で、ざっくり **日本語なら1文字＝1〜2トークン**、英語なら1単語＝1トークン強が目安です。長い文章・大きいファイルを渡すほどトークンを多く消費します。

トークンが効いてくる場面は2つあります。

| 場面 | 何に影響するか |
|---|---|
| **① コンテキストウィンドウ** | 1回の会話で覚えていられる量の上限（モデルにより20万〜100万トークン。最新のOpus/Sonnetは100万）。埋まると古い内容が自動で圧縮される（→ 3-4） |
| **② プランの利用上限** | 5時間枠・7日枠でどれだけ使ったか。使い切ると一定時間使えなくなる |

> [!tip] 今どれくらい使っているか見る方法
> - **`/context`** … 今の会話がコンテキストをどれだけ使っているかを内訳つきで表示
> - **statusLine（画面下部の常時表示）** … モデル名・コンテキスト使用率・プラン使用量などを常に表示できる。
>   `/statusline コンテキストの使用率をバーで表示して` のように日本語で頼むと自動で設定されます

> [!info] 実務での意味
> - 長い会話や大量のファイルを一度に渡すとトークンを浪費し、①圧縮（物忘れ）や②利用上限に早く到達します。
> - **作業の区切りで `/clear`**、**渡すファイルは絞る**、が節約の基本です（→ 3-4）。

### 3-4. モデル選択とコンテキスト管理

Claude Codeでは使うAIモデルを選べます。代表的なのは **Sonnet（標準・高速）** と **Opus（高性能）** です。`/model` コマンドで切り替えられます。

```
/model
```

> [!info] 「コンテキスト」とは
> Claudeは会話の履歴（これまでのやりとり・読み込んだファイル）を覚えながら作業します。この「覚えていられる範囲」がコンテキストです。
> 長く会話を続けるとこの範囲がいっぱいになり、Claudeが古い内容を**自動で圧縮（compaction）**します。圧縮されると、序盤に伝えた前提を忘れることがあります。

**モデルによる体感の違い：**

| | Sonnet | Opus |
|---|---|---|
| 速度 | 速い | やや遅い |
| 指示の理解精度 | 高い | より高い |
| やり直しの起きやすさ | 起きやすい | 起きにくい |
| コンテキストの埋まり方 | 早い（圧縮が起きやすい） | 遅い（圧縮が起きにくい） |

Opusは一発で意図を当てる精度が高く、やり直しや冗長な出力が減るため、**同じ作業でもコンテキストの消費が少なく、圧縮が起きにくい**傾向があります。さらに長い履歴の中から必要な情報を拾う精度も高いため、「前の話を忘れにくい」と感じられます。

> [!warning] よくある誤解
> 「Opusだから圧縮が起きない」のではありません。圧縮はあくまで**コンテキストの使用量**で決まります。Opusでも長時間1つのセッションを続ければいずれ圧縮されます。「Opusだとそこまで埋まりにくい」が正確な理解です。

> [!tip] コンテキストを溢れさせないコツ
> モデル選択に加えて、**作業の区切りごとに会話をリセット**するのが効果的です。
> ```
> /clear
> ```
> 1つの調査やスクリプト作成が終わったら `/clear` で履歴をクリアすると、そもそも圧縮が必要になりません。別件に移るときは新しい会話で始めるのが基本です。

> [!info] `/clear` しても会話は消えない（`/resume` で戻せる）
> `/clear` は「今の会話を新しいセッションに切り替える」操作で、**前の会話は保存されています**。削除ではありません。
> 前の作業に戻りたいときは **`/resume`** を実行すると、過去のセッション一覧から選んで**続きから再開**できます（ターミナルからは `claude --resume` でも可）。
> - 昨日の作業の続きを今日やる、別件に切り替えて後で戻る、といった**持ち越し**に便利です。
> - 注意：保存されたセッションは無期限ではなく、**既定で約30日**経過した古いものは自動削除されます。確実に残したい結論は**ファイル（手順書・メモ）に書き出して**おくのが安全です。

### 3-5. statusLineをWindowsで設定する（任意）

画面下部にトークン使用状況を**常時表示**する「statusLine」をWindowsで設定する手順です。**PowerShell**で動かします。

**手順1：スクリプトを作る**

メモ帳などで以下を `C:\Users\<ユーザー名>\.claude\statusline.ps1` として保存します（文字コードはUTF-8）。

```powershell
# Claude Codeが渡すJSON（セッション情報）を受け取る
$data = $input | Out-String | ConvertFrom-Json

$model = $data.model.display_name

# ① コンテキスト使用率（nullなら0扱い、0〜100に丸める）
$pct = [int]($data.context_window.used_percentage)
if ($pct -lt 0) { $pct = 0 } elseif ($pct -gt 100) { $pct = 100 }

# 使用率バー（10マス）
$filled = [math]::Floor($pct / 10)
$bar = ('#' * $filled) + ('-' * (10 - $filled))

$line = "[$model] $bar $pct% ctx"

# ② プラン使用量（Pro/Maxで、最初のAPI応答後にのみ現れる）
$fiveH = $data.rate_limits.five_hour.used_percentage
$week  = $data.rate_limits.seven_day.used_percentage
if ($null -ne $fiveH) { $line += (" | 5h: {0:N0}%" -f $fiveH) }
if ($null -ne $week)  { $line += (" | 7d: {0:N0}%" -f $week) }

Write-Host $line
```

**手順2：settings.jsonに登録する**

`C:\Users\<ユーザー名>\.claude\settings.json` に以下を追記します（ファイルが無ければ新規作成）。

```json
{
  "statusLine": {
    "type": "command",
    "command": "powershell -NoProfile -File C:/Users/<ユーザー名>/.claude/statusline.ps1"
  }
}
```

表示例：`[Opus] ###------- 28% ctx | 5h: 24% | 7d: 41%`

> [!warning] パスは「スラッシュ」で書く
> `command` のパスは Windows でも `C:/Users/...` と**スラッシュ区切り**で書いてください。
> `C:\Users\...` のように円マーク（バックスラッシュ）で書くと、内部処理でエスケープされて動かないことがあります。

> [!tip] 自分で書かなくてもよい
> Claude Codeに `/statusline コンテキスト使用率とプラン使用量を表示して` と日本語で頼めば、スクリプトとsettings.jsonを**自動生成**してくれます。
> 仕組みを理解して使うなら手動、手早く済ませたいなら `/statusline` が便利です。

---

## 4. 業務活用① ログ分析・障害調査

### 4-1. ログファイルを渡して分析させる

**基本パターン：**

```
このフォルダにある application.log を読んで、
エラーが発生している箇所を抽出し、原因と対処法を教えてください。
```

**より詳細な分析を求める場合：**

```
application.log を分析してください。
以下の観点でまとめてください：
1. エラーの種類と発生頻度
2. 最初にエラーが発生した時刻
3. 考えられる原因（上位3つ）
4. 推奨する対処手順
```

### 4-2. 実例：Windowsイベントログの読み解き

Windowsイベントビューアからエクスポートしたログ（.evtx または .txt）を渡す手順：

1. イベントビューアを開く（`eventvwr.msc`）
2. 対象のログを右クリック →「すべてのイベントをテキストファイルに保存」
3. 保存したファイルをClaude Codeの作業フォルダに置く
4. Claude Codeで指示

```
system_events.txt を読んで、直近24時間以内に発生したエラー・警告を
重大度順にまとめてください。特に繰り返し発生しているものを強調してください。
```

---

## 5. 業務活用② ドキュメント・手順書作成

### 5-1. 手順書をゼロから作る

```
以下の作業の手順書を作成してください。

【作業内容】新入社員のPC初期セットアップ
【対象OS】Windows 11 Pro
【実施者】IT部門担当者（技術的知識あり）
【形式】Markdown形式、スクリーンショットが必要な箇所はその旨を記載

含める内容：
- Windowsアクティベーション確認
- ドメイン参加手順
- 標準ソフトウェアのインストール（Chrome, Office, Zoom）
- セキュリティソフトの設定
- ネットワーク設定確認
```

### 5-2. 既存手順書の更新・改善

```
この手順書（setup_manual.md）を読んで、以下の改善をしてください：
1. 手順が曖昧な箇所を具体化する
2. 注意事項を目立つように整理する
3. 想定される失敗パターンとその対処法を末尾に追加する
```

### 5-3. 実例：キッティング手順書の作成

```
Windows 11のPCキッティング手順書を作成してください。

前提：
- 台数：月10〜20台
- 実施者：IT部門2名
- 使用ツール：MDM（Intune）、社内イメージ

含める項目：
- 事前準備チェックリスト
- キッティング手順（番号付きステップ）
- 動作確認チェックリスト
- よくあるトラブルと対処法

Markdown形式、チェックボックス（- [ ]）を使ってください。
```

---

## 6. 業務活用③ スクリプト生成

### 6-1. PowerShellスクリプトを作らせる

```
以下の要件でPowerShellスクリプトを作成してください。

【目的】複数PCのディスク使用率を一括確認
【入力】PCのホスト名リスト（hostnames.txt、1行1ホスト名）
【出力】各PCのCドライブ使用率をCSVファイルに保存
【条件】
- 接続できないPCはスキップしてエラーログに記録
- 管理者権限不要
- 文字コードはUTF-8
- コメントは日本語で記載
```

### 6-2. 生成スクリプトの確認方法

生成されたスクリプトを実行する前に、必ず以下を確認してください：

**確認チェックリスト**
- [ ] スクリプトの全体的な目的が意図通りか
- [ ] ファイルの削除・上書き処理がないか（または想定通りか）
- [ ] ネットワーク接続・外部通信がないか
- [ ] 管理者権限を要求していないか（要求する場合は必要性を確認）
- [ ] エラー発生時に適切に処理が止まるか

**Claudeにレビューさせる：**

```
このスクリプト（cleanup.ps1）を実行する前に安全性をレビューしてください。
特に以下を確認してください：
- 意図しないファイル削除がないか
- 実行範囲が想定通りか
- 危険な処理が含まれていないか
```

### 6-3. 実例：PC一括設定スクリプト

```
新規PCのセットアップ時に実行するPowerShellスクリプトを作成してください。

処理内容：
1. コンピュータ名の変更（引数で指定）
2. タイムゾーンを東京に設定
3. 電源設定をパフォーマンス優先に変更
4. Windowsアップデートの自動更新を無効化
5. 設定完了ログをC:\IT\logs\setup_log.txtに記録

要件：
- 管理者権限で実行
- 各ステップの成功・失敗をログに記録
- エラー発生時は処理を中断して原因を表示
```

---

## 7. Skillで繰り返し作業を技能化する

「キッティング手順」「ログ分析の観点」など、**毎回同じ指示を打ち込んでいる作業**は、**Skill（スキル）** として登録しておくと効率的です。

### 7-1. Skillとは

Skillは「特定の作業のやり方」をまとめたファイルです。普段は読み込まれず、**関連する作業のときだけ自動で読み込まれます**。

> [!tip] なぜCLAUDE.mdより良いのか
> よく使う手順を全部 `CLAUDE.md`（常時読まれる前提ファイル）に書くと、毎回その分トークンを消費し、無関係な情報でClaudeが散ります。
> Skillは**必要な時だけ読まれる**ので、手順をいくつ用意してもコンテキストを圧迫しません（→ 3-3 トークン）。

### 7-2. Skillの作り方

`プロジェクトフォルダ\.claude\skills\<スキル名>\SKILL.md` を作るだけです。先頭に name / description を書き、本文に手順を書きます。

```markdown
---
name: kitting
description: Windows 11 PCのキッティング手順。新規PCのセットアップ作業のときに使う。
---

# キッティング手順

以下の順で進め、各ステップの完了をログに残す。
1. コンピュータ名を命名規則に従って変更する
2. ドメインに参加する
3. 標準ソフト（Chrome / Office / Zoom）を導入する
4. セキュリティソフトを設定する
5. 動作確認チェックリストを実施する
```

> [!tip] Claudeに作らせる
> 手順書がすでにあるなら、こう頼めば変換してくれます：
> ```
> runbooks/kitting.md の内容を .claude/skills/kitting/SKILL.md としてSkill化してください。
> descriptionは「いつ使うか」が分かるように書いてください。
> ```

### 7-3. Skillの使い方

- **自動**：description に書いた状況になると、Claudeが自動で読み込んで使います（「新しいPCをセットアップして」と言えば kitting スキルが効く）。
- **手動**：`/kitting` のようにスラッシュで明示的に呼び出すこともできます。

> [!info] descriptionが命
> Claudeは description を見て「今これを使うべきか」を判断します。
> **「何をするか」だけでなく「いつ使うか」**を具体的に書くのがコツです（例：「新規PCのセットアップ作業のときに使う」）。

### 7-4. 何をSkillにすると良いか

- キッティング手順、退職者アカウント停止手順などの**定型オペレーション**
- ログ分析の観点（→ 4章）のような**毎回同じ切り口**
- PowerShellスクリプトの社内規約（命名・コメント・文字コード）

これらをSkill化してGitで共有すれば（→ 10章）、**チーム全員が同じ手順・同じ品質**でClaudeを使えます。

---

## 8. リポジトリ構成（チームで共有する単位）

成果物（スクリプト・手順書・Skill）をチームで共有するとき、**どう入れ物を分けるか**の方針です。結論は **「運用リポジトリを1つ作り、その中をフォルダで分ける」** が基本です。

### 8-1. Claude Codeの「プロジェクト」とは

Claude Codeでの「プロジェクト」は、**`claude` を起動したフォルダ**のことです。重い登録は不要で、そのフォルダ（と親フォルダ）にある設定を読むだけです。設定は**3層で自動的に合成**されます。

| 層 | 置き場所 | 用途 | 共有 |
|---|---|---|---|
| 個人・全体 | `~\.claude\` | 自分用（statusLine・既定モデル等）| しない |
| チーム共通 | リポジトリ直下の `.claude\` と `CLAUDE.md` | 共通ルール・ガードレール・Skill | gitで共有 |
| 種別・領域ごと | `.claude\rules\<名>.md`（`paths`で限定）| その種別／領域だけの規約 | gitで共有 |

つまり「サブプロジェクトの階層」は、**フォルダを掘って共通ルールを置く**だけで作れます。別リポジトリにする必要はありません。

> [!tip] CLAUDE.md / rules / Skill の使い分け
> 設定の置き場所は役割で選びます。
> - **`CLAUDE.md`**（常時読込）… プロジェクト全体で常に効かせたい前提・ルール。**短く保つ**。
> - **`.claude/rules/<名>.md`**（条件付き）… 先頭に `paths:` でファイルパターンを書くと、**該当ファイルを触るときだけ**読み込まれる。「PowerShellの規約」のような*種別ごとの規約*に最適。`paths` を書かないと常時読込になる点に注意。
> - **Skill**（→ 7章）… 「キッティング手順」などの*作業のやり方*。関連時 or `/名前` で呼ばれた時だけ読まれる。
>
> 例：PowerShellの規約は `.claude/rules/scripts.md` に `paths: ["**/*.ps1"]` を付けて置くと、`.ps1` を扱う時だけ自動で効きます。「特定フォルダで作業する時だけ効かせたい」なら、そのフォルダに `CLAUDE.md` を置く方法もあります（こちらはフォルダ単位）。

### 8-2. 推奨構成：1リポジトリ＋フォルダ階層

```
it-operations\                  ← gitリポジトリは「1つ」（チームで共有）
├─ CLAUDE.md                    ← チーム共通の前提・ルール（小さく保つ）
├─ .claude\
│  ├─ settings.json            ← 共有ガードレール（denyルール／statusLine：→ 11-4）
│  ├─ rules\
│  │  └─ scripts.md            ← PowerShell等の規約（pathsで .ps1 に限定）
│  └─ skills\                  ← 繰り返す手順を技能化（→ 7章）
│     ├─ kitting\SKILL.md
│     └─ log-analysis\SKILL.md
├─ scripts\                    ← PowerShell等の成果物
├─ runbooks\                   ← 手順書
└─ logs\                       ← ログ置き場
```

作業するときは、**その領域のフォルダで `claude` を起動**します。ルートの共通ルールと、その領域の `CLAUDE.md` が両方効きます。

### 8-3. なぜ「1リポジトリ」が良いか

| 案 | git共有 | 効率 | 評価 |
|---|---|---|---|
| 全部を1つのCLAUDE.mdに詰め込む | ◎ | ✗ 毎回全部読んでトークンを浪費 | △ |
| **1リポジトリ＋フォルダ階層** | ◎ | ◎ 必要な領域だけ読む | **◎ 推奨** |
| 領域ごとに別リポジトリ | ✗ クローン・pull/pushが複数系統で事故のもと | ◎ | △ |

特に**Git初心者のチーム**では、共有の単位を1リポジトリに統一すると、クローンも pull/push も1系統で済み、覚えやすく事故が減ります。

### 8-4. 別リポジトリに分けるべき例外

最初は1リポジトリで十分です。次のような状況になったら分割を検討します。
- 一部メンバーにだけ見せたい**機密手順**がある（アクセス境界が違う）
- 社外公開する物と社内専用が混在する（公開範囲が違う）
- 更新サイクルや責任者が完全に別

> [!tip] 迷ったらまず1つ
> 2〜5名の立ち上げ期は、まず `it-operations` 1リポジトリで始め、**痛くなってから割る**のが安全です。

---

## 9. VS Codeと組み合わせて使う

これまで学んだClaude Code（`claude` コマンド）は、**VS Code（無料のコードエディタ）の統合ターミナルから起動**すると、ファイル閲覧・差分確認・Git操作を1つの画面でこなせて便利です。**Claudeの操作は、これまでどおりCLI（ターミナル）に統一します。**

### 9-1. なぜVS Codeと組み合わせるのか

- リポジトリ（成果物一式）を**ツリーで見渡せる**
- Claudeが変更したファイルを**VS Codeの差分ビューアで確認**できる（統合ターミナルの `claude` が自動でVS Codeと連携）
- **Git操作を画面（Source Control）でも**できる（→ 9-4）
- 学んだ `claude`・`git` のコマンドを**そのまま統合ターミナルで**使える＝環境が1つにまとまる

### 9-2. 導入（Windows）

**① VS Code本体をインストール**

1. https://code.visualstudio.com を開き「Download for Windows」
2. インストーラを実行（**管理者権限不要**、既定設定のままでOK）

**② Claude Code拡張を入れる**

1. VS Codeで `Ctrl + Shift + X`（拡張機能ビュー）を開く
2. 「**Claude Code**」で検索 → **Install**

拡張を入れると、統合ターミナルで起動した `claude` がVS Codeと連携し、差分をVS Codeの差分ビューアで確認できるようになります。

> [!info] 前提
> VS Code は **1.98 以上**が必要です。拡張機能にはCLI（`claude` コマンド）も同梱されます。

### 9-3. 統合ターミナルからClaudeを起動する（標準）

1. 作業するリポジトリを VS Code で開く（**File → Open Folder** → `it-operations`）
2. 統合ターミナルを開く：`` Ctrl + ` ``
3. 作業フォルダで `claude` を起動：

```powershell
cd it-operations
claude
```

4. 初回はブラウザでサインイン（Session 1 で用意した**有料プランのアカウント**）

あとは Session 1〜2 で学んだCLI操作と同じです（指示・`/clear`・`/resume` など）。Claudeがファイルを変更するときは、**VS Codeの差分ビューアで内容を確認してから承認**できます。

### 9-4. VS CodeでGitを使う

Git操作（→ 10章）は、画面でもコマンドでも、どちらでもOKです。

- **Source Control（ソース管理）パネル**（左の枝アイコン）：変更ファイルと差分を見て、**ステージ → コミット → push / pull をボタン操作**で。
- **統合ターミナル**で、これまでの `git` コマンドをそのまま実行（→ 10章）。
- Claudeに「変更内容に合った日本語のコミットメッセージで commit して」と頼むこともできます。

### 9-5. グラフィカルな「Claudeパネル」について（非推奨）

VS Code拡張には、チャット形式の**グラフィカルなパネル**も付属します（右上のSpark ✱ アイコン等で開く）。ただし本ガイドでは**非推奨**とし、**Claudeの操作は統合ターミナルのCLIに統一**します。

理由：
- **長時間・常駐の処理に不向き**（バックグラウンド処理の可視性が弱い）
- **一部のコマンド／機能しか使えず、全用途をカバーできない**（`!` でのシェル実行・Tab補完・全スラッシュコマンドはCLIのみ）
- 上記から**汎用性に欠ける**ため、入口をCLIの1本に揃えた方がチームで教えやすく、運用も安定する

> [!note] うっかりパネルを開いたら
> 本運用では使いません。`` Ctrl + ` `` で統合ターミナルを開き、`claude` を使ってください。

---

## 10. Git / GitHubでチーム管理

### 10-1. なぜバージョン管理が必要か

Claude Codeで生成したスクリプトや手順書を管理せずに使い続けると、以下の問題が起きます：

- 誰かがスクリプトを修正したが、他のメンバーは古いバージョンを使い続ける
- 「以前のバージョンに戻したい」ときに元のファイルがない
- どのスクリプトが最新か分からなくなる

**Gitはこれらを解決するバージョン管理ツールです。**

### 10-2. 最初の準備（初回だけ）

チームのリポジトリは**すでにGitHub上にある前提**です。各メンバーは、それを自分のPCに複製（クローン）して使います。**最初の1回だけ**実行します。

```powershell
# Gitが入っているか確認
git --version

# 名前とメールを設定（このPCで1回だけ）
git config --global user.name "山田太郎"
git config --global user.email "yamada@company.com"

# チームのリポジトリを自分のPCに複製する
git clone https://github.com/組織名/it-operations.git

# 複製されたフォルダに移動（以降はこの中で作業）
cd it-operations
```

> [!info] cloneは「最初の1回だけ」
> 一度クローンすれば、次回からは `cd it-operations` で入って作業するだけです。クローンし直す必要はありません。

### 10-3. 日々の更新フロー（これだけ覚えればOK）

作業のたびに、この4ステップを順に回します。

```powershell
# 1. 最新を取り込む（他のメンバーの変更を反映：作業前に必ず）
git pull

#   → ここで Claude Code を使ってファイルを作成・修正

# 2. 変更を保存対象に追加する
git add .

# 3. コメントを付けて記録する（-m のあとにメッセージは必須）
git commit -m "ディスク使用率チェックのスクリプトを追加"

# 4. GitHubにアップして全員に共有する
git push
```

> [!warning] コミットには必ずコメントを付ける
> `git commit -m "..."` の **`-m` のあとに、何を変えたか分かる日本語のメッセージを必ず**入れてください。
> - `-m` を省くと、メッセージ入力用の別画面（エディタ）が開いて戸惑うもとになります。
> - 良い例：「キッティング手順書に動作確認チェックを追加」／悪い例：「修正」「update」「aaa」
>
> 後から履歴（`git log --oneline`）を見たとき、誰がいつ何をしたか分かるのが目的です。

> [!tip] コメントをClaudeに作らせる
> ```
> 今の変更内容に合ったgitのコミットメッセージを日本語で1つ提案してください
> ```

> [!warning] pushする前の確認
> パスワードや認証情報がファイルにハードコードされていないか確認してください。
> pushすると、リポジトリのメンバー全員が見られます（機密ファイルは `.gitignore` で除外：→ 8章）。

### 10-4. （担当者向け）リポジトリを新しく作る場合

普段使うメンバーは 9-2〜9-3 だけでOKです。**最初にリポジトリを用意する担当者だけ**、この手順を1回行います。

**① GitHubでリポジトリを作る**

1. github.com にアクセスしてアカウントを作成（持っていなければ）
2. 右上の「+」→「New repository」
3. リポジトリ名を入力（例：`it-operations`）
4. 「Private」を選択（社内用途のため）
5. 「Create repository」をクリック

**② チームメンバーを招待**

リポジトリの「Settings」→「Collaborators」→「Add people」で各メンバーを追加します。

**③ 手元のフォルダを最初に上げる場合**（例：用意した `it-operations` 雛形を上げる）

```powershell
cd it-operations
git init                       # フォルダをGit管理下に置く（最初の1回）
git add .
git commit -m "リポジトリの初期構成を追加"
git remote add origin https://github.com/組織名/it-operations.git
git push -u origin main
```

これ以降、他のメンバーは 9-2 の `git clone` から始められます。

### 10-5. AI駆動チーム運用フロー

```
【チームでのAI駆動運用イメージ】

担当A                          担当B・C
  │                              │
  │ 1. Claude Codeでスクリプト生成  │
  │ 2. 動作確認・修正               │
  │ 3. git add → git commit        │
  │ 4. git push → GitHubへアップ   │
  │                              │
  │                 5. git pull ←─┤
  │                 6. 最新スクリプトを取得
  │                 7. 自分のPCで実行
  │                              │
  │ ← 8. 改善点をClaude Codeで修正 │
  │ ← 9. git push                 │
  │                              │
         全員が常に最新版を使える状態
```

**このサイクルを回すことで、チーム全体がAIの成果物を共有・活用できます。**

---

## 11. 安全に使うために（詳細版）

### 11-1. 渡してはいけない情報（具体例）

| カテゴリ | 具体例 |
|---|---|
| 認証情報 | パスワード、ハッシュ値、秘密鍵、証明書 |
| 個人情報 | 氏名・住所・電話番号・メールアドレス（社員・顧客問わず） |
| 機密データ | 契約書、財務情報、未公開の製品情報 |
| システム情報 | 内部IPアドレス体系、ネットワーク構成図 |
| ライセンス | ソフトウェアのライセンスキー、シリアル番号 |

**対処法：** 渡す前にファイル内の機密情報を `[REDACTED]` や `****` に置き換える。

### 11-2. 出力を鵜呑みにしない判断基準

**信頼度が高い場面：**
- 一般的なPowerShell構文の生成
- エラーメッセージの意味の説明
- 手順書のドラフト作成

**必ず人間が確認すべき場面：**
- 実際に実行するスクリプト（特に削除・変更を伴うもの）
- セキュリティ設定の変更
- 本番環境への適用

### 11-3. 権限モードの仕組みと危険性

Claude Codeは「権限モード」で操作の承認方法を切り替えます。主なモードは以下です。

| モード | 動作 | IT業務での推奨 |
|---|---|---|
| **default（通常）** | Bashコマンドとファイル編集の前に確認を求める（読み取りは確認不要） | ✅ 推奨 |
| **acceptEdits** | ファイルの編集を自動承認する | ⚠️ 内容を理解できる人のみ |
| **plan** | 読み取りのみ。ファイルを書き換えない（調査・計画づくり用） | ✅ 安全 |
| **bypassPermissions** | すべての確認をスキップ（`--dangerously-skip-permissions`） | ❌ 使用禁止 |

> [!info] 読み取りは確認なしが正常
> ファイルの読み込みや検索（Grep）などの「読むだけ」の操作は、もともと確認なしで実行されます。
> 確認を求められるのは **Bashコマンドの実行** と **ファイルの編集・作成** です。

> [!tip] モードの切り替え方
> セッション中に **`Shift + Tab`** を押すとモードが順番に切り替わります（通常 → 編集自動承認 → プランモード …）。
> 今どのモードかは画面下部の表示で確認できます。

> [!danger] 危険なモードに入ってしまった場合
> 編集が確認なしで進む状態になったら、**`Shift + Tab` を何回か押して「通常（default）」に戻して**ください。
> 分からなくなったら `Ctrl + C` でClaudeを終了し、再起動すれば通常モードに戻ります。
> `bypassPermissions`（全確認スキップ）はIT業務では使わないでください。

### 11-4. 権限の確認とチームでのガードレール共有

**今なにを許可しているか確認する：** セッション中に `/permissions` と入力すると、許可・確認・拒否のルール一覧が表示されます。

**危険なコマンドをあらかじめ禁止する：** プロジェクトフォルダの `.claude/settings.json` に拒否ルールを書いておくと、Claudeがそのコマンドを実行しようとしても止められます。

```json
{
  "permissions": {
    "deny": [
      "Bash(git push *)",
      "PowerShell(Remove-Item *)"
    ]
  }
}
```

> [!tip] チームでガードレールを共有できる
> この `.claude/settings.json` はGitでバージョン管理できます（Session 3参照）。
> リポジトリに含めてpushすれば、**チーム全員が同じ禁止ルールを共有**でき、危険な操作を組織として防げます。

---

## 12. プロンプト例集（早見表）

### ログ分析用

```
【ファイル名】.log を分析して、
エラーの発生箇所・原因・推奨対処法を箇条書きでまとめてください。
```

```
このエラーメッセージの意味と、Windowsで起こりうる原因を教えてください：
[エラーメッセージをここに貼り付け]
```

```
直近1週間のログから、繰り返し発生しているエラーパターンを特定してください。
優先度の高い順に並べて、各エラーへの対処法も提案してください。
```

### 手順書作成用

```
【作業名】の手順書をMarkdown形式で作成してください。
対象者：IT担当者
チェックリスト形式（- [ ]）を使い、注意事項はcalloutで強調してください。
```

```
この手順書（ファイル名）を改善してください。
曖昧な手順を具体化し、よくあるつまずきポイントを追記してください。
```

### スクリプト生成用

```
以下の処理をするPowerShellスクリプトを作成してください。
- [処理内容]
- 管理者権限：[要/不要]
- エラー時の挙動：[処理停止/スキップして継続]
- ログ出力先：[パス]
- コメント：日本語で記載
```

```
このスクリプト（ファイル名）を読んで、何をしているか日本語で説明してください。
特に副作用（ファイル変更・削除・ネットワーク通信）がある箇所を強調してください。
```

### コミットメッセージ生成用

```
以下のスクリプトの変更内容に適したgitコミットメッセージを日本語で3案作ってください：
[変更内容の概要]
```

---

> [!info] 困ったときは
> - Claude Code公式ドキュメント：https://docs.anthropic.com/ja/docs/claude-code
> - `/help` コマンド：Claude Code内で使えるコマンド一覧を表示
> - `Ctrl + C`：応答が止まったとき・やり直したいときに使用
